version: '3'

tasks:
  test:
    desc: "Run tests with coverage"
    cmds:
      - go test -mod=mod -cover ./...

  build:
    desc: "Build the module"
    cmds:
      - go build -mod=mod ./...

  lint:
    desc: "Vet the module"
    cmds:
      - go vet -mod=mod ./...